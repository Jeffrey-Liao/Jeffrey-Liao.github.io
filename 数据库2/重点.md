# 存储结构
![](pic/20260109195548.png)
- *Table Space*包含若干个*Segments*
- Segment由若干个Extents组成
	- 每个表**对应一个段**（Segment），用于存放**该表的所有数据**
- Extents由若干个Data Blocks组成
	- 每个 **extent 必须位于一个 data file** 内（不能跨文件）
- Data Block 一般与Disk Block一一对应
![](pic/Pasted%20image%2020260110213826.png)
![](pic/20260109194602.png)

---
# 数据字典视图
1. **`ALL_` 视图**
    - 包含当前用户**有权限访问的所有对象**的信息。这意味着如果某个对象对用户是可见的或可访问的，无论该对象是否为当前用户所有，相关信息都会出现在 `ALL_` 视图中。
2. **`USER_` 视图**
    - 仅包含当前用户所**拥有的对象的信息**。这些视图提供了一个更为狭窄的视角，专注于用户的自有资源。
3. **`DBA_` 视图**
    - 提供了数据库中**所有对象的全面视图**，但访问这些视图通常需要额外的权限，如DBA角色。它们**允许管理员查看整个数据库中的所有对象**，而不局限于任何单个用户。

---
# 数据文件
## Heap File
![](pic/Pasted%20image%2020260110160745.png)
1. **简单结构**  
    数据直接追加到文件末尾，无排序、无索引。
2. **高效插入**  
    新记录直接插入到文件末尾，无需移动其他记录，因此插入操作非常快（O(1)）。
3. **低效查询**
    - **等值查询**：需要扫描整个文件，平均读取一半的页才能找到目标记录。
    - **范围查询**：必须读取所有页。
    - **删除操作**：通常需要标记删除或移动记录，效率较低。

---
## Sorted files
![](pic/Pasted%20image%2020260110160758.png)
>是一种**按照某个字段（排序字段）有序存储记录**的文件组织方式，记录在物理上是按顺序排列的，通常用于提高查询效率。
1. **排序字段（Ordering Field）**  
    记录按照这个字段的值进行排序，例如按员工姓名（Ename）排序。    
2. **排序键字段（Ordering Key Field）**  
    如果排序字段**恰好也是唯一键**（如员工号 Empno），则称为“排序键字段”。  
    例如：`Empno` 是唯一键，如果文件按 `Empno` 排序，则它既是排序字段又是键字段。

### 优点
- **快速查找（对数级搜索）**  
	- 因为记录是有序的，可以使用**二分查找**，时间复杂度为 **O(log n)**，远快于堆文件的 O(n)。
- **高效范围查询**  
	- 例如查询工资在 3000-5000 之间的员工，只需定位到起始位置后顺序读取，无需全表扫描。
- **适合频繁查询、少更新的场景**

### 缺点
- **插入和删除效率低**  
	- 插入新记录时必须保持顺序，可能需移动大量记录；删除也可能需要移动记录以保持连续。
- **更新开销大**  
	- 修改排序字段的值可能导致记录位置变动，类似重新插入。

---
## Hashed files
![](pic/Pasted%20image%2020260110161155.png)
>通过**哈希函数**将记录分配到不同的“桶”（bucket）中，从而实现快速的查找、插入和删除操作
1. **选择哈希键**：  
    - 指定文件中的某一个字段作为**哈希键**（hash key），比如学生的学号、订单编号等。
2. **记录定位**：  
    - 对于一条记录，计算其哈希键的哈希值 h(K)，然后将该记录存入第 h(K) 号桶中。
3. **搜索操作**：  
    - 要查找某个键 K ，只需计算 h(K) ，然后直接访问对应的桶即可。由于不需要遍历整个文件，因此**查找效率非常高**（平均时间复杂度接近 O(1)）。
4. **插入与删除**：
    - 插入：计算哈希值，放入对应桶。
    - 删除：找到对应桶，移除目标记录。
5. **冲突处理（Collision Handling）**：  
    - 当多个记录哈希到同一个桶时，会发生**冲突**（collision）。解决方法包括：
	    - 使用**溢出页（Overflow Page）**：当桶满时，将多余记录存入额外的溢出页面，并用链表连接。
	    - 每个桶可以链接一个溢出链表，所有溢出记录按顺序排列。

---
# Bitmap
## 单条件where
从表格中筛选指定条件的数据

| Empno | Status   | Region  | Gender | Info      |
| ----- | -------- | ------- | ------ | --------- |
| 101   | single   | east    | male   | bracket_1 |
| 102   | married  | central | female | bracket_4 |
| 103   | married  | west    | female | bracket_2 |
| 104   | divorced | west    | male   | bracket_4 |
| 105   | single   | central | female | bracket_2 |
| 106   | married  | central | female | bracket_3 |

| number | REGION='east'          | REGION='central'          | REGION='west'          | 求**AND**获得where的结果 |
| ------ | ---------------------- | ------------------------- | ---------------------- | ------------------ |
| 1      | **1**(east == east)    | **0**(central == east)    | **0**(west == east)    | 0                  |
| 2      | **0**(east == central) | **1**(central == central) | **0**(west == central) | 0                  |
| 3      | **0**(east == west)    | **0**(central == west)    | **1**(west == west)    | 0                  |
| 4      | **0**(east == west)    | **0**(central == west)    | **1**(west == west)    | 0                  |
| 5      | **0**(east == central) | **1**(central == central) | **0**(west == central) | 0                  |
| 6      | **0**(east == central) | **1**(central == central) | **0**(west == central) | 0                  |

## Between
![](pic/Pasted%20image%2020260110164906.png)

---
# B+树删除
![](pic/Pasted%20image%2020251209161915.png)

| 节点类型   | 最大指针数量 | 最大键数量 | 最小指针数量                  | 最小键数量                   |
| ------ | ------ | ----- | ----------------------- | ----------------------- |
| 非叶非根节点 | n+1    | n     | $\lceil(n+1)/2\rceil-1$ | $\lceil(n+1)/2\rceil-1$ |
| 叶节点    | n+1    | n     | $\lfloor n/2\rfloor$    | $\lfloor n/2\rfloor$    |
| 根节点    | n+1    | n     | 1                       | 1                       |
$\lfloor4/2\rfloor =2$

所以删除后需要借节点。
合并叶节点
```
[10,20,30,35,40]
```
分裂成
```
[10,20,30] [35,40]
```
更新父节点的分隔键
```
[10,35,100]
```

---
# 表格类型
| 表类型                             | 描述                             |
| ------------------------------- | ------------------------------ |
| Ordinary (heap-organized) table | 数据以**无序集合**（heap）方式存储          |
| **Partitioned** table           | 数据被**划分为**更小、更易管理的**片段**       |
| Index-organized table (**IOT**) | 数据（包括非键值）**按 B-tree 索引结构排序**存储 |
| Clustered table                 | 来自**多个表的相关数据**物理上存储**在一起**     |
## 什么是分区
partition is:
- **A piece of** a “very large” **table** or index
- Stored in its **own segment**
- Used for **improved performance** and manageability
- **Operations on partitioned tables** and indexes can be **performed in parallel** by assigning different parallel execution servers to different partitions of the table or index.

---
## 分区规则
| Partition Type | Description                                      | Example                     |
| -------------- | ------------------------------------------------ | --------------------------- |
| Range          | Based on **ranges of column values**             | `sales_date < '2023-01-01'` |
| Hash           | Uses **hash function to distribute** rows evenly | `MOD(customer_id, 4)`       |
| List           | Explicit **mapping of discrete values**          | `region IN ('EU', 'NA')`    |

---
# T(R)识别
![](pic/Pasted%20image%2020260110173230.png)
$T(R)$是总的记录数量
$L(R)$是每条记录的长度=20+4+8+5=37
$V(R,A)$是A中**唯一**的数据的数量(cat,dog,bat) = 3
$V(R,B)$是B中**唯一**的数据的数量(1) = 1
$V(R,C)$是C中**唯一**的数据的数量(10,20,30,40,50) = 5
$V(R,D)$是D中**唯一**的数据的数量(a,b,c,d) = 4

---
# 查询开销计算
## 关键运算
- $T_R$,$N_R$: **number of records** in R (other notation: $T_R$ or $T(R)\to tuple$)
- $L_R$: **size of record** in R (length of record-> L(R))
- $bf_R$: **blocking factor** (other notation: $F_R$ or $bf(R)$) 
	- number of records in a page (data block)
- $B_R$: **number of pages** to store relation $R (\to B(R)$)
- $V(R, A)$: **number of distinct values** of attribute A in R other notation: $I_A(R)$ (Image size) or V(A,R)
- $SC(R,A)$: **selection cardinality** of A in R (number of matching rec.) 
	- A key: $SC(R, A)$=1 
	- ==A non key: $SC(R, A)=T_R / V(R,A)$ (uniform distribution assumption)==
- $HT_i$: **number of levels in index** I (-> height of tree)
- **rounding up** fractions and logarithm

---
## Nested loop join
>S,R为不同的两个关系
- Best
$$B_R+B_S$$
- Worst
$$T_R * B_S + B_R$$

---
## Block nested loop join
>S,R为不同的两个关系
- Best
$$B_R+B_S$$
- Worst
$$B_R * B_S + B_R$$

---
## Sort-Merge cost

$$cost\:of\:sorting + B_S + B_R$$


---
## Indexed nested loop join
- $c$ :the cost of **index-based selection of inner relation**
$$B_R + T_R * c$$
---
## Hash join
- B: number of pages
$$2*(B_R+B_S) + (B_R+B_S)$$
---
# 结果大小估计
- 当关系运算$W=R*S$
	- 总元组数量: $T(W) = T(R) * T(S)$
	- 新单条记录的长度: $L(W) = L(R) + L(S)$
	- 单个块中的记录数量: $bf(W) = b/(L(R)+L(S))$

---
# Optimizer
- **Cost-Based Optimizer (CBO)**
	- 当前默认且唯一支持的优化器。
	- 基于**统计信息**（statistics）估算不同执行计划的“代价”（cost），选择代价最低的方案。
- **Rule-Based Optimizer (RBO)**
	- 基于一组预定义的规则选择执行计划。

---
# 执行计划
## 1. **理解操作符执行顺序**
- `MINUS/UNION/INTERSECT` → 先`SORT UNIQUE`两个子查询
- `EXISTS`还有`having` → 通常用`FILTER`或`NESTED LOOPS`
- `OR`条件 → 可能用`CONCATENATION`（并集）或`INLIST ITERATOR`
- 等值查询 + 唯一索引 → `INDEX UNIQUE SCAN`
    
---
## 2. **识别访问路径**
- 全表扫描：`TABLE ACCESS FULL`
- 通过索引定位：`INDEX [UNIQUE/RANGE] SCAN`
- 通过ROWID访问表：`TABLE ACCESS BY ROWID`

| **特性**     | **Index Unique Scan** | **Index Range Scan**              |
| ---------- | --------------------- | --------------------------------- |
| **查询类型**   | 等值查询（=）               | 范围查询（>, <, BETWEEN, LIKE）或非唯一索引等值 |
| **返回值**    | 最多1行                  | 可能多行                              |
| **索引要求**   | 唯一索引                  | 任何索引                              |
| **性能**     | O(1)时间复杂度             | O(log n) + 扫描范围                   |
| **执行计划显示** | `INDEX UNIQUE SCAN`   | `INDEX RANGE SCAN`                |

---
## 3. 语句与执行计划的关系
### TABLE ACCESS full
- **单表**且**没有索引**
- **单表**且**没有索引**且有where

### SORT order by
- 出现`ORDER BY`

### INDEX full scan 索引名称
- 对操作的列建立了索引
	- 且使用了**排序**
	- 选择只选择建立了索引的列

### INDEX unique scan 索引名称
- 对操作的列建立了*Unique*索引，且使用了**匹配**(=)

### INDEX range scan 索引名称
- 对操作的列建立了索引，且使用了**匹配**(=)，或者范围匹配

### SORT group by
- 使用了`group by` 

### FILTER
- 使用了`HAVING`

### TABLE ACCESS by rowid emp
- 当创建了*Unique index*且使用多个列查询，且其中一个列是unique index。

### INDEX range scan A，INDEX range scan B
- 当使用多个列查询，且两个列都是普通索引

### NESTED LOOPS
- 从不止一个表中select数据

### JOIN
1. **`MERGE JOIN`**：表示使用的是“合并连接”算法。
2. **`SORT join`**：说明两个表的数据都会先进行排序。

3. **Inner Join**, 只有一半进行索引时
```
>.SELECT STATEMENT
>...NESTED LOOPS
>.....TABLE ACCESS full dept
>.....TABLE ACCESS by rowid emp
>.......INDEX range scan e_emp_fk
```
5. **Inner Join**, 且两边都有索引
```
>.SELECT STATEMENT
>...NESTED LOOPS
>.....TABLE ACCESS full dept
>.....TABLE ACCESS by rowid emp
>.......INDEX range scan e_emp_fk
```

---
# Codd12律
1. Information Rule
	>The data stored in a database, may it be **user data** or **metadata**, must be a **value of some table cell**. Everything in a database must be **stored in a table format**.
	- 所有数据在数据库中必须是表结构数据，包括用户数据和模式（结构）数据
2. Guaranteed Access Rule
	>Every single **data element (value)** is guaranteed to be **accessible** logically with a combination of *table-name*, *primary-key* (row value), and *attribute-name* (column value). No other means, such as *pointers*, can be used to access data。
	- 所有数据都可以通过**表名称**，**主键（行值）**，和**属性名称(列值)** 通过**指针**访问到。
3. Systematic Treatment of NULL Values
	>The **NULL values** in a database must be given a **systematic and uniform treatment**. This is a very important rule because a NULL can be interpreted as one of the following：
	>- data is *missing*, 
	>- data is *not known,* 
	>- or data is *not applicable*
	- 数据库中的**NULL**必须被给予系统且统一的解决方式，数据只能在以下几种情况被解释为NULL：
		- **数据丢失**
		- **数据未知**
		- **数据不可用**
4. Active Online Catalog
	> The structure description of the entire database must be stored in an **online catalog**, known as **data dictionary**, which can be *accessed by authorized users*. **Users** can use the same query language to **access the catalog** which they use to **access the database itself**.
	- 数据库的整体结构描述必须存储在一个在线目录中，称为**数据字典**（data dictionary）记录了**数据库的所有结构信息**，该目录**可被授权用户访问**。用户可以使用与访问数据库本身相同的查询语言来访问这个目录。
	- **数据字典**是数据库系统内部存储元数据的地方，比如：
	    - 表结构
	    - 索引
	    - 视图
	    - **完整性约束**
5. **Comprehensive Data Sub-Language Rule**
	>There must be at least **one language** whose statements are *expressible*, per some *well-defined syntax*, as character strings and that is *comprehensive in supporting* all of the following items:
		> - **Data definition.** (create table, alter table, drop table)
		> - **View definition.** (create view)
		> - **Data manipulation** (insert, delete, update).
		> - **Integrity constraints.** (primary key, foreign key, check)
		> - **Authorization.** (grant, revoke)
		> - **Transaction boundaries** (commit, rollback, savepoint)
6. View Updating Rule
	>All the **views** of a database, which can theoretically be updated, **must also be updatable by the system**
	- 数据库中所有**理论上可以被更新的视图**，**系统也必须支持对其进行更新**。DBMS 应该允许通过这个视图插入、修改或删除数据
7. High-Level Insert, Update, and Delete Rule
	> A **database** **must support** high-level *insertion*, *update*, and *deletion*. This must not be limited to a single row, that is, it must also support *union*, *intersection*, and *minus* operations to yield **sets of data records**.
	- 数据库必须支持**插入，更新，删除**语句，且必须在数据集上支持**并，交，差**等集合操作。
8. Physical Data Independence
>The **data stored in a database** must be **independent** **of the applications** that access the database. ==Any change in the physical structure of a database must not have any impact on how the data is being accessed by external applications.==
	- 对于数据库中的**数据**，其必须**与访问数据库的应用独立**。所有**物理结构的修改**必须**不影响**已建立的**外部应用访问**
9. Logical Data Independence
>The **logical data in a database** must be **independent of its user’s view** (application). ==Any change in logical data must not affect the applications using it==. For example, if *two tables are merged* or one is *split into two different tables*, there should be *no impact or change on the user application*.
	- 数据库中的**逻辑数据**必须与其用户的视图（即应用程序）**相互独立**。  对**逻辑数据结构**的任何**更改**，**不应影响使用它的应用程序**。  例如，如果两个表被合并，或一个表被拆分为两个表，用户应用程序**不应受到任何影响**。
	- 逻辑数据：
		- **逻辑结构**，比如：
			- 表名、字段名
			- 表之间的关系（外键）
			- 分区方式、索引设计等
10. Integrity Independence
>**Integrity constraints** specific *to a particular relational data* base must be ==definable in the relational data sublanguage and storable in the catalog==, **not in the application programs**.
	- 针对某个特定关系型数据库的**完整性约束**，必须能够用**关系数据子语言**（如 SQL）定义，并存储在**数据目录（catalog）中，而不能写在应用程序代码里**。
11. Distribution Independence
>**The end-user** must **not** be able to *see that the data is distributed over various locations*. Users should always get the impression that the data is located at one site only. This rule has been regarded as **the foundation of distributed database systems**
	- **最终用户不应察觉到数据分布在多个地理位置上。**  用户始终应该感觉数据只存储在一个地方。  这条规则被认为是**分布式数据库系统的基础**。
12. Non-Subversion Rule
>If a **system** has an interface that **provides access to low-level records**, then ==the interface must **not** be able to **subvert** the system and **bypass** security and integrity **constraints.**==
	- 如果一个系统提供了访问**低级别记录**的接口，那么这个接口**不能绕过系统的安全性和完整性约束**。  换句话说：**即使能直接操作底层数据，也不能破坏规则**。

---
# 锁
| 锁类型                                       | 含义                     |
| ----------------------------------------- | ---------------------- |
| **S** (Shared)                            | 共享锁，读操作                |
| **X** (Exclusive)                         | 排他锁，写操作                |
| **IS** (Intent Shared)                    | 意图共享锁，表示后续可能对子节点加 S 锁  |
| **IX** (Intent Exclusive)                 | 意图排他锁，表示后续可能对子节点加 X 锁  |
| **SIX** (Shared with Intent to eXclusive) | 共享锁 + 意图排他，即当前读，但打算后续写 |

![](pic/Pasted%20image%2020251216145300.png)

## 不能相容的锁
- IS
	- X
- IX
	- S
	- SIX
	- X
- S
	- IX
	- SIX
	- X
- SIX
	- 除了IS
- X
	- 所有锁

---
# Log文件
- `<T , START>`：This record indicates that transaction **T has begun**. 
- `<T , COMMIT >`：**Transaction** T has **completed** successfully and will make no more changes to database elements.
- `<T, ABORT >`：Transaction T **could not complete** successfully.
- `<T, X, v>`：Transaction T has **changed database element X** , and its former value was v

---
## Redo Log
>处理all the **committed transactions** in the order *earliest-first*
1. S表示事物集合（transaction set）with
	- `<Ti,commit>`(and **no** `<Ti, end>`) in log
2. For each `<Ti, X, v>` in log, **in forward order** (earliest → latest) do
	- if $Ti \in S$ then `Write(X, v)` `Output(X)`
3. For each $Ti \in S$, write `<Ti, end>`

---
## Modified Redo Log
1. S表示事物集合（transaction set）with
	- `<Ti,commit>`(and **no** `<Ti, end>`) in log
2. For each <Ti, X, v> in log, **in forward order** (earliest → latest) do
	- if $Ti \in S$ then `Write(X, v)` `Output(X)`
3. For each $Ti \notin S$, write `<Ti, abort>`

### 读取文件
**If we first meet** an `<END CKPT>` record, we need to scan no further back than the **earliest of** `<START Ti>` records (among corresponding `<START CKPT(T1, T2, … Tk)>`).

If we first meet an `<END CKPT>` record:
- When we **redo a transaction**, we do not need to look **prior to the** `<START CKPT>`record, because we know that changes prior to the start of the checkpoint were **flushed to disk during the checkpoint.**

If we first meet a `<START CKPT> `record:
- We simply ignore it, find `<END CKPT>`.


### 检查点
- **non-quiescent checkpoint**
1. Write log record `<START CKPT(T1, …Tk)>` T1, …Tk are **active (uncommitted) transactions**, and flush log. 
2. ==Write to disk all database elements== that were **written to buffers** but **not yet to disk** by transactions that had already committed when the record was written to the log. (dirty buffers) 
3. Write an `<END CKPT>` record to the log and flush the log

---
## Undo Log
>处理all the **incomplete transactions** in the order *latest-first*.
1. Page X can be flushed before or after Ti commit 
2. Log record flushed before corresponding updated page (WAL) 
3. Flush at commit (log only)

### 读取文件
If we first meet an `<END CKPT>` record: 
- However **when we undo a transaction**, we have to look **prior to the** `<START-CKPT>` record to find if there were actions that may have **reached disk and need to be undone**. (e.g. T2) 

If we first meet a `<START CKPT>` record: We simply ignore it, find `<END CKPT>`.

### 检查点
- **non-quiescent checkpoint**
1. Write log record `<START CKPT(T1, …Tk)>`T1 … Tk are active (uncommitted) transactions, and flush log.
2. Write to disk all the buffers that are dirty; i.e., they contain one or more changed database elements. **Unlike redo logging,** we **flush all dirty buffers**, not just those written by committed transactions.
3. Write a log record `<END CKPT>` and flush the log

---
# 并行原理
## Conflict equivalent
>$S_1, S_2$ are **conflict equivalent schedules** if $S_1$ can be *transformed into* $S_2$ by a series of ==non-conflicting swaps of adjacent actions==

---
## Conflict Serializable
>A **schedule** is **conflict serializable** if *it is conflict equivalent to some serial schedule*
- 如果一个调度是冲突可串行化的，那么它一定是可串行化的（对所有初始值都成立）。
- 但是，一个调度可能不是冲突可串行化的，却仍然在**某些特定初始值下**是可串行化的（即结果等价于某个串行调度）

---
## 定理
>1. S1, S2 conflict equivalent → P(S1)=P(S2)
>2. P(S1)=P(S2) ↛ S1, S2 conflict equivalent
>3. P(S1) acyclic ↔ S1 conflict serializable

# Hashing and Indexing
## Hashing - Dynamic Hashing
### Linear
核心思想
![](pic/Pasted%20image%2020260110210111.png)
1. Use i low order bits of hash
2. File grows linearly
![](pic/Pasted%20image%2020260110210123.png)

### Extensible

---
## Indexing vs Hashing
1. Hashing good for **probes given key**
2. INDEXING (Including B Trees) good for **Range Searches**

| 特性 | 索引（Index） | 哈希（Hashing） |
|------|------------------|--------------------|
| 目的 | 快速定位满足条件的行（用于 `WHERE` 过滤或 `JOIN`） | 主要用于 等值连接（Equi-Join） 的高效实现（Hash Join），或在内存中快速去重/分组（如 `HASH GROUP BY`） |
| 适用操作 | `WHERE col = value`、范围查询、前缀匹配等 | 仅适用于等值条件（`=`），不能用于 `<`, `>`, `LIKE` 等 |
| 数据访问方式 | 随机 I/O（通过 B-tree 导航到数据块） | 全表扫描 + 内存哈希表构建（对小表）+ 探测（对大表） |
| 是否需要预先存在 | 需要 DBA 显式创建索引 | 无需预先创建，由优化器在执行时动态构建 |

---

# B+ Tree
## 非叶节点
- 度(n)：为节点的最大键数量
- 指针数量：n+1
![](pic/Pasted%20image%2020260110162353.png)

---
## 叶节点
![](pic/Pasted%20image%2020260110162433.png)
直接指向键指向的记录

---
## 最小节点键和指针数量
- 非叶节点：$\lceil (n+1)/2\rceil\:pointers$
	- 最小节点数：1
- 叶节点：$\lfloor(n+1)/2\rfloor\:pointers$
	- 最小节点数：2

---
## B+树规则
- All leaves **at same lowest level**
- Pointers in **leaves point to records** except for “sequence pointer”
	- next leaf

| 节点类型          | 最大指针数量 | 最大键数量 | 最小指针数量                  | 最小键数量                   |
| ------------- | ------ | ----- | ----------------------- | ----------------------- |
| 非叶非根节点        | n+1    | n     | $\lceil(n+1)/2\rceil-1$ | $\lceil(n+1)/2\rceil-1$ |
| 叶节点           | n+1    | n     | $\lfloor n/2\rfloor$    | $\lfloor n/2\rfloor$    |
| 根节点           | n+1    | n     | 1                       | 1                       |
| Root non-leaf | n      | n     | 1                       | 1                       |
| Root leaf     | n      | n     | 1                       | 1                       |

### 插入行为
![](pic/Pasted%20image%2020260110163435.png)
当节点键数量大于n时，进行分裂，找$n/2$下标的键提到父节点中，并把$0\to n/2-1$分裂到一个新的节点。

### 建立新根节点
插入45
![](pic/Pasted%20image%2020260110163715.png)

![](pic/Pasted%20image%2020260110163739.png)

### 删除行为
![](pic/Pasted%20image%2020260110163852.png)
删除键值50（n=4，最小键数=2）
删除前：
	叶子节点1: \[40,50]      兄弟节点: \[10,20,30,60]
          ↓ 删除50
	叶子节点1: \[40]         → 键数=1 < 2
重新分配：
	叶子节点1: \[40,60]      兄弟节点: \[10,20,30]
父节点更新：60上移替换原分隔键

# 复习题
- [选择](sample_quiz.html)
- [填空](blank.html)