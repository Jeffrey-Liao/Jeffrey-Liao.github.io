# 拓扑序
有向无环图（DAG）中，对每个顶点进行线性排序，使得每条有向边 `u→v` 中，`u` 都出现在 `v` 之前

# 逆拓扑序
将拓扑序完全反转得到的序列

# 计算方法
## 求拓扑序再反转

## 基于出度的Kahn算法
- 逆拓扑序：不断删除**出度为0**的节点

## DFS算法获取逆拓扑序
- DFS后序遍历（Post-order）：先访问子节点，再访问当前节点
- **回溯时**将节点加入结果，自动得到逆拓扑序
